name: Ph3ntom-Spoofer-Complete

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  build:
    runs-on: windows-2022
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2
        with:
          vs-version: '[17.0, 18.0)'
      
      - name: Detect WDK Paths
        shell: pwsh
        run: |
          $wdkBase = "${env:ProgramFiles(x86)}\Windows Kits\10"
          $includePath = "$wdkBase\Include"
          $libPath = "$wdkBase\Lib"
          
          if (-not (Test-Path $includePath)) {
            Write-Error "WDK Include path not found: $includePath"
            exit 1
          }
          
          $versionDirs = Get-ChildItem -Path $includePath -Directory | 
                         Where-Object { $_.Name -match "^\d+\.\d+\.\d+\.\d+$" } | 
                         Sort-Object Name -Descending
          
          if ($versionDirs.Count -eq 0) {
            Write-Error "No WDK version directories found"
            Get-ChildItem -Path $includePath -Directory | Select-Object Name
            exit 1
          }
          
          $wdkVersion = $versionDirs[0].Name
          $kmPath = "$includePath\$wdkVersion\km"
          $ntddkPath = "$kmPath\ntddk.h"
          
          if (-not (Test-Path $ntddkPath)) {
            Write-Error "ntddk.h not found at: $ntddkPath"
            exit 1
          }
          
          $wdkInc = "$includePath\$wdkVersion"
          $wdkKmInc = "$kmPath"
          $wdkLib = "$libPath\$wdkVersion\km\x64"
          
          "WDK_VERSION=$wdkVersion" | Out-File -FilePath $env:GITHUB_ENV -Append
          "WDK_INC=$wdkInc" | Out-File -FilePath $env:GITHUB_ENV -Append
          "WDK_KM_INC=$wdkKmInc" | Out-File -FilePath $env:GITHUB_ENV -Append
          "WDK_LIB=$wdkLib" | Out-File -FilePath $env:GITHUB_ENV -Append
      
      - name: Build
        shell: cmd
        run: |
          call "${{ env.VS_INSTALL_PATH }}\VC\Auxiliary\Build\vcvars64.bat"
          
          set INCLUDE=%WDK_KM_INC%;%WDK_INC%\shared;%INCLUDE%
          set LIB=%WDK_LIB%;%LIB%
          
          if not exist "%WDK_KM_INC%\ntddk.h" (
            echo [ERROR] ntddk.h not found
            exit /b 1
          )
          
          mkdir bin
          cd extracted\hwid-spoofer\driver
          msbuild driver.vcxproj /p:Configuration=Release /p:Platform=x64 /p:OutDir=..\..\..\bin\ /nologo /m /p:WindowsTargetPlatformVersion=10.0
          cd ..\mapper
          msbuild mapper.vcxproj /p:Configuration=Release /p:Platform=x64 /p:OutDir=..\..\..\bin\ /nologo /m
      
      - name: Download Intel Driver
        shell: cmd
        run: |
          powershell -Command "Invoke-WebRequest -Uri 'https://github.com/Ch0pin/medusa/raw/master/iqvw64e.sys' -OutFile 'bin/iqvw64e.sys' -UseBasicParsing"
      
      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: ph3ntom-binaries
          path: bin/
